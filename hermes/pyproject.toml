[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "stira-hermes"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
macos = ["pyobjc-framework-Cocoa>=10.0", "pyobjc-framework-ApplicationServices>=10.0"]
dev = ["pytest>=8.0"]
