// gemini.ts (NO API calls, just returns a simple message)

export const generatePersonalizedValue = async (problem: string): Promise<string> => {
  return "Stira filters YouTube distractions so you can lock in and learn faster.";
};
